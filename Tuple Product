def multiply_tuple(tuple_numbers):
  product = 1
  for number in tuple_numbers:
    product *= number
  return product
my_tuple = (2, 3, 4, 5)
result = multiply_tuple(my_tuple)
print("Product of numbers in the tuple:", result)
